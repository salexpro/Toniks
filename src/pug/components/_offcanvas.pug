#menu.off-canvas.position-left(data-off-canvas data-transition="overlap" data-auto-focus="false")
    .off-canvas_logo
        img(src="./img/logo.svg", alt="Tonik's")
    +menu('vertical')
    button.button.primary.icon(data-open="appointment")
        | Записаться
        +icon('arr', 24, 16)
    button.close-button.small(aria-label='Закрыть', type='button', data-close)
        span(aria-hidden='true') ×